import { BaseAsset } from "../../../internal";
import { DefaultPortfolio, NumberedPortfolio, SectionPermissions, SimplePermissions, TransactionPermissions, TxTag } from "../../../types";
/**
 * Calculate the difference between the required Transaction permissions and the current ones
 */
export declare function getMissingPortfolioPermissions(requiredPermissions: (DefaultPortfolio | NumberedPortfolio)[] | null | undefined, currentPermissions: SectionPermissions<DefaultPortfolio | NumberedPortfolio> | null): SimplePermissions['portfolios'];
/**
 * @hidden
 *
 * Calculate the difference between the required Asset permissions and the current ones
 */
export declare function getMissingAssetPermissions(requiredPermissions: BaseAsset[] | null | undefined, currentPermissions: SectionPermissions<BaseAsset> | null): SimplePermissions['assets'];
/**
 * @hidden
 *
 * Calculate the difference between the required Transaction permissions and the current ones
 */
export declare function getMissingTransactionPermissions(requiredPermissions: TxTag[] | null | undefined, currentPermissions: TransactionPermissions | null): SimplePermissions['transactions'];
//# sourceMappingURL=helpers.d.ts.map